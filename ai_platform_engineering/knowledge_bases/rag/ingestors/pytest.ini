[pytest]
testpaths = tests
# `common` installs as a copy rather than an editable link, so without its source
# on the path a test run silently exercises whatever was vendored at install time.
pythonpath = src ../common/src
addopts = -v --tb=short
asyncio_mode = auto
